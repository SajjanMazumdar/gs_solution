import { RestrictlengthDirective } from './restrictlength.directive';

describe('RestrictlengthDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictlengthDirective();
    expect(directive).toBeTruthy();
  });
});
